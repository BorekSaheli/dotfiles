#!/usr/bin/env sh

# Cross-platform commit-msg hook managed by chezmoi.
# Enforces commit message format. Silent on success.

set -eu

# Source shared git-rules
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
. "$SCRIPT_DIR/git-rules"

validate_commit_msg "$1"
