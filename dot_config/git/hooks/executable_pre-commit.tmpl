#!/usr/bin/env sh

# Cross-platform pre-commit hook managed by chezmoi.
# Enforces branch naming conventions. Silent on success.

set -eu

# Source shared git-rules
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
. "$SCRIPT_DIR/git-rules"

validate_branch
